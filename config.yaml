# GitHub Trending Daily Configuration

# 抓取的语言分类列表（空字符串表示所有语言）
# 可选值: "", "python", "javascript", "typescript", "go", "rust", "java", "cpp" 等
language_list: [""]

# 时间范围: daily, weekly, monthly
since: "daily"

# 输出目录
json_dir: './json'
md_dir: './md'
md_readme_path: 'README.md'
